import React from 'react'

function Loading() {
    return (
        <div className='flex justify-center items-center min-h-screen'>
            <button className=' px-16 text-brand font-extrabold text-lg py-4 rounded-full' disabled>Loading ...</button>
        </div>
    )
}

export default Loading
